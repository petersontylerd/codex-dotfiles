# schema_version: 1.0.0
metadata:
  feature_id: ""                   # e.g., "F001" (sequential within epic)
  parent_epic: ""                  # e.g., "E001" (must match parent epic ID)
  feature_name: ""
  schema_version: "1.0.0"

overview:
  description: ""                  # 2–4 sentence summary of functionality/value
  user_value: ""                   # why users care
  in_scope: []                     # [string] functionality included
  out_of_scope: []                 # [string] functionality explicitly excluded

dependencies:
  internal_features: []            # other feature IDs
  external_dependencies: []        # vendor/API/platform dependencies

success_criteria:
  - description: ""                # what success looks like
    target: ""                     # measurable success signal
    notes: ""

risks_assumptions:
  - description: ""
    type: ""                       # tech | data | ops | adoption
    mitigation: ""
    owner: ""

prioritization:
  priority: ""                     # Must | Should | Could | Won't
  rationale: ""